# Pi dependencies — install with: pip install -r requirements.txt
#
# Note: picamera2 and RPi.GPIO are Raspberry Pi specific and will not
# install on a regular workstation. Install them on the Pi only.
#
# torch must be installed separately from the official Pi wheel:
#   https://pytorch.org/get-started/locally/
# or via the community ARM wheel:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

RPi.GPIO>=0.7.1
picamera2>=0.3.12
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
numpy>=1.23.0
opencv-python>=4.7.0
keyboard>=0.13.5
