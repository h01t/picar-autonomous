# Workstation dependencies — install with: pip install -r requirements.txt
#
# For GPU training, install PyTorch with CUDA support instead:
#   https://pytorch.org/get-started/locally/
# e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
torchvision>=0.15.0
albumentations>=1.3.0
scikit-learn>=1.2.0
pandas>=1.5.0
numpy>=1.23.0
opencv-python>=4.7.0
matplotlib>=3.6.0
Pillow>=9.0.0
